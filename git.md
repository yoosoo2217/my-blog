---
title: Git
layout: default
---

# Git

(이 파일은 예전 Just the Docs 구조에서 쓰던 카테고리 인덱스 페이지로, `_config.yml`의 `exclude` 목록에 포함되어 지금은 빌드에서 제외됩니다.)

Git과 GitHub를 배우면서 정리한 글은 이제 `_posts/YYYY-MM-DD-제목.md` 형식으로 저장하고 `tags: [Git]`을 붙여서 관리합니다. Git 관련 글 목록은 [/tag/git/]({{ site.baseurl }}/tag/git/) 페이지에서 볼 수 있습니다.